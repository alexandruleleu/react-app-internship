//import { actionTypes } from "./ActionsHome";

export default function homeReducer(
    state = {
        count: 0,
    },
    action,
) {
    switch (action.type) {
        default:
            return state;
    }
}
